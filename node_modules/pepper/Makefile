SRC = $(shell find ./lib ./examples ./test -name "*.js" -type f)
MOCHA = $(shell find ./node_modules -type l -name "mocha")
JSHINT = $(shell find ./node_modules -type l -name "jshint")

lint:
	@$(JSHINT) $(SRC)

test:
	@$(MOCHA)

.PHONY: test lint