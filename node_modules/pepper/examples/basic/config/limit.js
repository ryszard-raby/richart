// Module dependencies
var express = require('express');

// Express config
function configure(app) {
  app.use(express.limit('300kb'));
}
configure.priority = 2;

// Exports
module.exports = configure;