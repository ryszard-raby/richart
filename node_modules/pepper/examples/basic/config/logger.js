// Module dependencies
var express = require('express');

// Express config
function configure(app) {
  app.use(express.logger(app.get('devel') ? 'dev' : 'normal'));
}
configure.priority = 3;

// Exports
module.exports = configure;