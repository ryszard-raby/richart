// Module dependencies
var express = require('express');

// Express config
function configure(app) {

  // Base middleware
  app.use(express.bodyParser());
  app.use(express.methodOverride());

  // Development flag
  app.set('devel', app.settings.env === 'development');
}
configure.priority = 1;

// Module exports
module.exports = configure;