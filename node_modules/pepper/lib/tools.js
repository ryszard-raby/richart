/*!
 * Pepper
 * Copyright (c)2012 Pierre Matri <pierre.matri@coolony.com>
 * MIT Licensed
 */


/**
 * Capitalize first letter of `str` and return the result.
 *
 * @param {String} str String to capitalize.
 * @return {String} Capitalized string.
 * @api private
 */

exports.capitalizeFirstLetter = function(str) {
  if(!str || !str.length) return str;
  return [str.charAt(0).toUpperCase(), str.slice(1)].join('');
};


/**
 * Module exports
 */

module.exports = exports;